<h2>Edit game user</h2>

<%= render "form.html", changeset: @changeset,
                        action: game_user_path(@conn, :update, @game_user) %>

<%= link "Back", to: game_user_path(@conn, :index) %>
